class Course {
  final String ? title;
  final String ? teacherName;
  final String ? teacherImage;
  final String ? coursePrice;
  final String ? numberOfLessons;
  final String ? courseDuration;
  Course({this.title,this.teacherName,this.teacherImage,this.coursePrice,this.numberOfLessons,this.courseDuration});
}